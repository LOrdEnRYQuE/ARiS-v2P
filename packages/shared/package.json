{
  "name": "@aris/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for ARiS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["aris", "shared", "types", "utilities"],
  "author": "ARiS Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
